package com.example.demo.common.excel.engine.metadata;

public enum ColumnType {
    STRING,
    INTEGER,
    LONG,
    SHORT,
    BYTE,
    FLOAT,
    DOUBLE,
    BIG_DECIMAL,
    BIG_INTEGER,
    BOOLEAN,
    CHARACTER,
    LOCAL_DATE,
    LOCAL_DATE_TIME,
    LOCAL_TIME,
    DATE,
    ENUM,
    OBJECT
}
